<header class="bg-dark text-white">
    <nav class="navbar navbar-expand-lg navbar-dark container">
        <a class="navbar-brand font-weight-bold" href="@Url.Action("Index","Home")">IKart</a>

        <!-- Search -->
        <form class="form-inline mx-auto w-50" action="@Url.Action("Search","Products")" method="get">
            <input class="form-control w-75" type="text" name="q" placeholder="Search for products..." />
            <button class="btn btn-warning ml-2" type="submit"><i class="fa fa-search"></i></button>
        </form>

        <!-- User Actions -->
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link text-white" href="@Url.Action("Login","Account")"><i class="fa fa-user"></i> Login</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white" href="@Url.Action("Cart","Orders")"><i class="fa fa-shopping-cart"></i> Cart</a>
            </li>
        </ul>
    </nav>
</header>
