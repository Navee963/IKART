<header class="admin-topbar d-flex align-items-center justify-content-between px-3 py-2" style="background-color:#34495e;">
    <form class="search-bar flex-grow-1 mx-3" method="get" action="@Url.Action("Index","Orders", new { area = "" })">
        <div class="input-group input-group-sm">
            <input class="form-control" name="q" placeholder="Search orders, products, customers..." style="border-radius:20px 0 0 20px; border:1px solid #ccc;" />
            <div class="input-group-append">
                <button class="btn btn-primary" type="submit" style="border-radius:0 20px 20px 0;">
                    <i class="fas fa-search"></i>
                </button>
            </div>
        </div>
    </form>

    <ul class="nav align-items-center mb-0">
        <li class="nav-item dropdown mr-3">
            <a class="nav-link dropdown-toggle text-white p-2" href="#" id="notifDropdown" data-toggle="dropdown">
                <i class="fas fa-bell"></i>
                <span class="badge badge-light badge-pill text-primary">2</span>
            </a>
            <div class="dropdown-menu dropdown-menu-right p-2" aria-labelledby="notifDropdown" style="min-width:260px;">
                <div class="small text-muted">Notifications</div>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item small" href="#">New order received (#12345)</a>
                <a class="dropdown-item small" href="#">Tokens Raised</a>
            </div>
        </li>
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white p-2" href="#" id="profileDropdown" data-toggle="dropdown">
                <i class="fas fa-user-circle"></i> Admin
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="profileDropdown">
                <a class="dropdown-item" href="#">Profile</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#">Sign out</a>
            </div>
        </li>
    </ul>
</header>

<style>
    .search-bar .form-control {
        font-size: 0.95rem;
    }

    .search-bar .btn-primary {
        background-color: #3498db;
        border: none;
    }

        .search-bar .btn-primary:hover {
            background-color: #2980b9;
        }

    .nav-link {
        transition: background 0.2s ease, color 0.2s ease;
    }

        .nav-link:hover {
            background: rgba(255,255,255,0.2);
            border-radius: 6px;
            color: #fff !important;
        }
</style>
